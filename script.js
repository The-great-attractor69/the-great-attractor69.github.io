(()=>{const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const ro=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.08});$$('.reveal').forEach(x=>ro.observe(x));
addEventListener('scroll',()=>{let m=document.documentElement.scrollHeight-innerHeight;$('#progress').style.width=(m?scrollY/m*100:0)+'%'},{passive:true});
const nav=[...document.querySelectorAll('.nav nav a')],secs=[...document.querySelectorAll('main section[id]')];new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)nav.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+e.target.id))}),{rootMargin:'-30% 0px -55%'}).observe;secs.forEach(s=>new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)nav.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+e.target.id))}),{rootMargin:'-30% 0px -55%'}).observe(s));
const co=new IntersectionObserver((es,o)=>es.forEach(e=>{if(!e.isIntersecting)return;let x=e.target,t=+x.dataset.count,s=x.dataset.suffix||'',st=performance.now();(function f(n){let p=Math.min((n-st)/900,1),q=1-(1-p)**3;x.textContent=Math.round(t*q)+s;p<1&&requestAnimationFrame(f)})(st);o.unobserve(x)}),{threshold:.8});$$('[data-count]').forEach(x=>co.observe(x));
$$('.project').forEach(p=>{let bs=$$('.tabs button',p),cs=$$('.tab[data-content]',p);bs.forEach(b=>b.onclick=()=>{bs.forEach(x=>x.classList.remove('active'));cs.forEach(x=>x.classList.remove('active'));b.classList.add('active');let c=p.querySelector(`[data-content="${b.dataset.tab}"]`);if(c)c.classList.add('active')})});
const projects=$$('.project');function filter(skill){skill=skill.toLowerCase();$('#filter').textContent=skill==='all'?'all projects':skill;projects.forEach(p=>{let a=(p.dataset.skills||'').split(',').map(x=>x.trim()),m=skill==='all'||a.some(x=>x.includes(skill)||skill.includes(x));p.classList.toggle('dim',!m&&skill!=='all');p.classList.toggle('highlight',m&&skill!=='all')});if(skill!=='all')$('#work').scrollIntoView({behavior:'smooth'})}$('#clear').onclick=()=>filter('all');$('#allProjects').onclick=()=>filter('all');
const canvas=$('#network'),ctx=canvas.getContext('2d'),visual=$('#heroVisual'),tip=$('#tooltip'),hint=$('#hint');let w=0,h=0,mouse={x:-9e3,y:-9e3,on:false},hover=null;const nodes=[['power bi','power bi',.72,.25,33,'20+ dashboards'],['excel','excel',.23,.28,31,'30+ dashboards'],['dax','dax',.79,.48,28,'analytical logic'],['qms','qms',.22,.65,29,'quality management'],['iqc','iqc',.74,.73,29,'incoming quality'],['audit','audit',.48,.17,28,'8 audit rounds'],['reporting','reporting',.51,.83,32,'management reporting'],['data','data management',.5,.5,58,'data → insight']].map(a=>({name:a[0],skill:a[1],x:a[2],y:a[3],r:a[4],desc:a[5],core:a[0]==='data'}));
function resize(){let r=canvas.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);w=r.width;h=r.height;canvas.width=w*d;canvas.height=h*d;ctx.setTransform(d,0,0,d,0,0)}function pos(n,t){return{x:n.x*w+(n.core?0:Math.sin(t*.0005+n.x*13)*7),y:n.y*h+(n.core?0:Math.cos(t*.00045+n.y*17)*6)}}function hit(x,y){for(let i=nodes.length-1;i>=0;i--){let p=pos(nodes[i],performance.now());if(Math.hypot(p.x-x,p.y-y)<nodes[i].r+10)return nodes[i]}return null}
function draw(t){ctx.clearRect(0,0,w,h);let ps=nodes.map(n=>({n,...pos(n,t)})),c=ps.find(p=>p.n.core);if(c){let g=ctx.createRadialGradient(c.x,c.y,5,c.x,c.y,180);g.addColorStop(0,'rgba(141,224,170,.13)');g.addColorStop(1,'rgba(141,224,170,0)');ctx.fillStyle=g;ctx.beginPath();ctx.arc(c.x,c.y,180,0,Math.PI*2);ctx.fill()}ps.forEach((a,i)=>ps.forEach((b,j)=>{if(j<=i)return;let d=Math.hypot(a.x-b.x,a.y-b.y),max=Math.min(w,h)*.46;if(d>max)return;let prox=mouse.on?Math.max(0,1-Math.min(Math.hypot(a.x-mouse.x,a.y-mouse.y),Math.hypot(b.x-mouse.x,b.y-mouse.y))/170):0;ctx.strokeStyle=`rgba(141,224,170,${Math.max(0,1-d/max)*.28+prox*.45})`;ctx.lineWidth=1+prox;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}));if(mouse.on)ps.forEach(p=>{let d=Math.hypot(p.x-mouse.x,p.y-mouse.y);if(d<145){ctx.strokeStyle=`rgba(141,224,170,${(1-d/145)*.55})`;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(mouse.x,mouse.y);ctx.stroke()}});ps.forEach(p=>{let n=p.n,r=n.r+(hover===n?7:0);if(n.core){let g=ctx.createRadialGradient(p.x-10,p.y-12,5,p.x,p.y,r);g.addColorStop(0,'#d8f5df');g.addColorStop(.35,'#8de0aa');g.addColorStop(1,'#2d8e5a');ctx.shadowColor='rgba(141,224,170,.42)';ctx.shadowBlur=30;ctx.fillStyle=g;ctx.beginPath();ctx.arc(p.x,p.y,r,0,Math.PI*2);ctx.fill();ctx.shadowBlur=0;ctx.fillStyle='#06100c';ctx.textAlign='center';ctx.textBaseline='middle';ctx.font='600 12px IBM Plex Sans';ctx.fillText('data',p.x,p.y-2);ctx.font='9px IBM Plex Sans';ctx.globalAlpha=.65;ctx.fillText('signal / insight',p.x,p.y+15);ctx.globalAlpha=1}else{ctx.beginPath();ctx.fillStyle=hover===n?'rgba(141,224,170,.16)':'rgba(255,255,255,.045)';ctx.strokeStyle=hover===n?'rgba(141,224,170,.75)':'rgba(202,231,214,.22)';ctx.arc(p.x,p.y,r,0,Math.PI*2);ctx.fill();ctx.stroke();ctx.fillStyle=hover===n?'#edf5ef':'#b5c4bc';ctx.textAlign='center';ctx.textBaseline='middle';ctx.font='500 10px IBM Plex Sans';ctx.fillText(n.name,p.x,p.y)}});requestAnimationFrame(draw)}
canvas.onpointermove=e=>{let r=canvas.getBoundingClientRect();mouse.x=e.clientX-r.left;mouse.y=e.clientY-r.top;mouse.on=true;hover=hit(mouse.x,mouse.y);if(hover){hint.textContent=hover.name+' · click to filter';tip.textContent=hover.desc;tip.style.display='block';tip.style.left=Math.min(mouse.x+12,w-145)+'px';tip.style.top=Math.max(mouse.y-35,45)+'px';canvas.style.cursor='pointer'}else{hint.textContent='hover a node';tip.style.display='none';canvas.style.cursor='default'}};canvas.onpointerleave=()=>{mouse.on=false;hover=null;tip.style.display='none';hint.textContent='hover a node'};canvas.onclick=e=>{let r=canvas.getBoundingClientRect(),n=hit(e.clientX-r.left,e.clientY-r.top);if(n&&!n.core)filter(n.skill)};addEventListener('resize',resize);resize();requestAnimationFrame(draw);
const slider=$('#slider'),lv=$('#loadValue'),pr=$('#processing'),qu=$('#queue'),ac=$('#accuracy'),state=$('#state'),logs=$('#logs');function log(text,type='ok'){let p=document.createElement('p');p.className='log';p.innerHTML=`<span class="time">[${new Date().toLocaleTimeString([],{hour12:false})}]</span> <span class="${type}">${text}</span>`;logs.prepend(p);while(logs.children.length>5)logs.lastElementChild.remove()}function load(v,write=true){v=+v;lv.textContent=v+'%';pr.textContent=Math.max(55,Math.round(99-v*.42))+'%';qu.textContent=Math.round(4+v*.72);ac.textContent=Math.max(78,Math.round(99-v*.17))+'%';let c=v>=75?'red':v>=50?'amber':'';state.className='state '+c;state.querySelector('span').textContent=v>=75?'critical':v>=50?'elevated':'stable';if(write)log(v>=75?'load threshold exceeded · intervention required':v>=50?'elevated load detected · monitoring queue':['ingesting quality records...','validating incoming dataset...','reconciling source records...','refreshing management view...'][Math.floor(Math.random()*4)],c==='red'?'err':c==='amber'?'warn':'ok')}slider.oninput=e=>load(e.target.value);log('system initialized · monitoring active');load(slider.value,false);
const modal=$('#cli'),input=$('#input'),output=$('#output');function open(){modal.classList.add('open');setTimeout(()=>input.focus(),50)}function close(){modal.classList.remove('open')}$('#openCli').onclick=open;$('#heroCli').onclick=open;$('#closeCli').onclick=close;modal.onclick=e=>e.target===modal&&close();document.onkeydown=e=>{if(e.key==='~'||(e.key==='`'&&!modal.classList.contains('open')))open();if(e.key==='Escape')close()};
function esc(s){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function command(raw){let c=raw.trim();if(!c)return;output.insertAdjacentHTML('beforeend',`<p><span class="muted">atta@portfolio:~$</span> ${esc(c)}</p>`);let a=c.toLowerCase().split(/\s+/),x=a[0],res={help:'available: help · skills · projects · about · contact · resume · load 0-100 · clear',skills:'power bi · advanced excel · dax · qms · iqc · internal auditing · data management · reporting',projects:'dppm dashboard · 30+ excel dashboards · incoming rejection reconciliation · qms documentation system · quality reporting architecture',about:'atta ullah | data visualization, quality systems, internal auditing and analytics.',contact:'email: attaullahkhokhar23@gmail.com'};if(x==='clear')output.innerHTML='';else if(x==='load'){let v=Math.max(0,Math.min(100,+a[1]||34));slider.value=v;load(v);output.insertAdjacentHTML('beforeend',`<p class="ok">simulated load set to ${v}%</p>`)}else if(x==='resume'){output.insertAdjacentHTML('beforeend','<p class="ok">opening resume.pdf</p>');let z=document.createElement('a');z.href='resume.pdf';z.download='atta-ullah-resume.pdf';z.click()}else output.insertAdjacentHTML('beforeend',`<p class="${res[x]?'ok':'warn'}">${res[x]||'command not found. type help.'}</p>`);output.scrollTop=output.scrollHeight}$('#command').onsubmit=e=>{e.preventDefault();command(input.value);input.value=''};
const found=new Set(),ach=$('#achievement'),achText=$('#achievementText');let conf=false;new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;if(['simulator','work','audit','skills','contact'].includes(e.target.id)){found.add(e.target.id);achText.textContent=`${found.size}/5 sections discovered`;ach.classList.add('show');clearTimeout(ach.timer);ach.timer=setTimeout(()=>ach.classList.remove('show'),2200);if(found.size===5&&!conf){conf=true;ach.querySelector('strong').textContent='exploration complete';achText.textContent='5/5 · hidden contact bonus unlocked';confetti()}}}),{threshold:.25});['simulator','work','audit','skills','contact'].forEach(id=>{let e=document.getElementById(id);new IntersectionObserver(es=>es.forEach(x=>{if(!x.isIntersecting)return;if(['simulator','work','audit','skills','contact'].includes(x.target.id)){found.add(x.target.id);achText.textContent=`${found.size}/5 sections discovered`;ach.classList.add('show');clearTimeout(ach.timer);ach.timer=setTimeout(()=>ach.classList.remove('show'),2200);if(found.size===5&&!conf){conf=true;ach.querySelector('strong').textContent='exploration complete';achText.textContent='5/5 · hidden contact bonus unlocked';confetti()}}}),{threshold:.25}).observe(e)});
function confetti(){let c=$('#confetti'),x=c.getContext('2d');c.width=innerWidth;c.height=innerHeight;let p=Array.from({length:90},()=>({x:innerWidth/2,y:innerHeight*.3,vx:(Math.random()-.5)*9,vy:-Math.random()*8-2,s:Math.random()*5+2,r:Math.random()*6,l:1}));let f=0;(function draw(){x.clearRect(0,0,c.width,c.height);p.forEach(a=>{a.x+=a.vx;a.vy+=.18;a.y+=a.vy;a.r+=.1;a.l-=.009;x.save();x.globalAlpha=Math.max(0,a.l);x.translate(a.x,a.y);x.rotate(a.r);x.fillStyle=['#8de0aa','#cbeed7','#e7bd67','#72c7e7'][f%4];x.fillRect(-a.s/2,-a.s/2,a.s,a.s*1.8);x.restore()});if(f++<120)requestAnimationFrame(draw);else x.clearRect(0,0,c.width,c.height)})()}
$$('[data-placeholder]').forEach(a=>a.onclick=e=>{e.preventDefault();open();command(a.dataset.placeholder)});$('#resume').onclick=()=>{let a=document.createElement('a');a.href='resume.pdf';a.download='atta-ullah-resume.pdf';a.click()};
})();
